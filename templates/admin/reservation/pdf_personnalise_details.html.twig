<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Réservation Personnalisée #{{ reservation.IDReservationPersonalise }}</title>
    <style>
        body {
            font-family: Arial, Helvetica, sans-serif; /* System font fallback */
            margin: 0;
            padding: 0;
            color: #333;
            background: #f8f9fa;
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
            background: #ffffff;
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.15);
            border-radius: 15px;
        }
        .header {
            text-align: center;
            padding: 20px 0;
            border-bottom: 1px solid #dee2e6;
            background: #fff;
        }
        .header img {
            max-width: 150px;
        }
        .header h1 {
            font-size: 28px;
            color: #007bff;
            margin: 10px 0 5px;
            font-weight: 600;
        }
        .header span {
            font-family: cursive; /* Fallback for Sacramento */
            font-size: 20px;
            color: #007bff;
            display: block;
        }
        .content {
            padding: 20px;
        }
        .content h2 {
            font-size: 24px;
            color: #2c3e50;
            text-align: center;
            margin-bottom: 20px;
            font-weight: 600;
        }
        dl {
            margin: 0;
            color: #2c3e50;
        }
        dt {
            font-weight: 700;
            font-size: 16px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-bottom: 8px;
        }
        dd {
            font-size: 15px;
            font-weight: 500;
            margin-bottom: 20px;
            padding: 10px 15px;
            background: #f8f9fa;
            border-radius: 5px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        th, td {
            padding: 10px;
            border: 1px solid #dee2e6;
            text-align: left;
        }
        th {
            background: #f0f0f0;
            font-weight: 700;
            text-transform: uppercase;
        }
        .footer {
            text-align: center;
            padding: 10px 0;
            border-top: 1px solid #dee2e6;
            font-size: 12px;
            color: #6c757d;
            background: #fff;
        }
        @media print {
            body {
                background: #fff;
            }
            .container {
                box-shadow: none;
                border: 1px solid #dee2e6;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <img src="file:///C:/Users/asus/Desktop/pidev/EventoraAppWEB/public/assets/img/logo-ct-dark.png" alt="Eventora Logo">
            <span>Votre Événement</span>
            <h1>Réservation Personnalisée #{{ reservation.IDReservationPersonalise }}</h1>
        </div>
        <div class="content">
            <h2>Détails de la Réservation</h2>
            <dl>
                <dt>Nom</dt>
                <dd>{{ reservation.nom|default('N/A') }}</dd>
                <dt>Prénom</dt>
                <dd>{{ reservation.prenom|default('N/A') }}</dd>
                <dt>Email</dt>
                <dd>{{ reservation.email|default('N/A') }}</dd>
                <dt>Numéro de téléphone</dt>
                <dd>{{ reservation.numtel|default('N/A') }}</dd>
                <dt>Description</dt>
                <dd>{{ reservation.description|default('N/A') }}</dd>
                <dt>Date</dt>
                <dd>{{ reservation.date ? reservation.date|date('Y-m-d') : 'N/A' }}</dd>
            </dl>
            <h2>Services Sélectionnés</h2>
            <table>
                <tr>
                    <th>Titre</th>
                    <th>Prix</th>
                    <th>Location</th>
                </tr>
                {% if reservation.services is not null and reservation.services|length > 0 %}
                    {% for service in reservation.services %}
                        <tr>
                            <td>{{ service.titre|default('N/A') }}</td>
                            <td>{{ service.prix|default('N/A') }}</td>
                            <td>{{ service.location|default('N/A') }}</td>
                        </tr>
                    {% endfor %}
                {% else %}
                    <tr>
                        <td colspan="3" style="text-align: center;">Aucun service sélectionné</td>
                    </tr>
                {% endif %}
            </table>
        </div>
        <div class="footer">
            <p>© 2025 Eventora. Tous droits réservés. | Généré le {{ 'now'|date('Y-m-d H:i:s') }}</p>
        </div>
    </div>
</body>
</html>