<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Réservation Pack #{{ reservation.IDReservationPack }}</title>
    <style>
        body {
            font-family: Arial, Helvetica, sans-serif;
            margin: 0;
            padding: 0;
            color: #333;
            background: #f8f9fa;
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
            background: #ffffff;
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.15);
            border-radius: 15px;
        }
        .header {
            text-align: center;
            padding: 20px 0;
            border-bottom: 1px solid #dee2e6;
            background: #fff;
        }
        .header h1 {
            font-size: 28px;
            color: #007bff;
            margin: 10px 0 5px;
            font-weight: 600;
        }
        .header span {
            font-family: cursive;
            font-size: 20px;
            color: #007bff;
            display: block;
        }
        .content {
            padding: 20px;
        }
        .content h2 {
            font-size: 24px;
            color: #2c3e50;
            text-align: center;
            margin-bottom: 20px;
            font-weight: 600;
        }
        dl {
            margin: 0;
            color: #2c3e50;
        }
        dt {
            font-weight: 700;
            font-size: 16px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-bottom: 8px;
        }
        dd {
            font-size: 15px;
            font-weight: 500;
            margin-bottom: 20px;
            padding: 10px 15px;
            background: #f8f9fa;
            border-radius: 5px;
        }
        .footer {
            text-align: center;
            padding: 10px 0;
            border-top: 1px solid #dee2e6;
            font-size: 12px;
            color: #6c757d;
            background: #fff;
        }
        @media print {
            body {
                background: #fff;
            }
            .container {
                box-shadow: none;
                border: 1px solid #dee2e6;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <span>Votre Événement</span>
            <h1>Réservation Pack #{{ reservation.IDReservationPack }}</h1>
        </div>
        <div class="content">
            <h2>Détails de la Réservation</h2>
            <dl>
                <dt>Nom du Pack</dt>
                <dd>{{ reservation.pack.nomPack|default('N/A') }}</dd>
                <dt>Nom</dt>
                <dd>{{ reservation.nom|default('N/A') }}</dd>
                <dt>Prénom</dt>
                <dd>{{ reservation.prenom|default('N/A') }}</dd>
                <dt>Email</dt>
                <dd>{{ reservation.email|default('N/A') }}</dd>
                <dt>Numéro de téléphone</dt>
                <dd>{{ reservation.numtel|default('N/A') }}</dd>
                <dt>Description</dt>
                <dd>{{ reservation.description|default('N/A') }}</dd>
                <dt>Date</dt>
                <dd>{{ reservation.date ? reservation.date|date('Y-m-d') : 'N/A' }}</dd>
            </dl>
        </div>
        <div class="footer">
            <p>© 2025 Eventora. Tous droits réservés. | Généré le {{ 'now'|date('Y-m-d H:i:s') }}</p>
        </div>
    </div>
</body>
</html>