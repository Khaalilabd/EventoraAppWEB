<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Réservation Pack #{{ reservation.IDReservationPack }}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            color: #333;
            background: #f8f9fa;
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
            background: #ffffff;
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.15);
            border-radius: 15px;
        }
        .header {
            text-align: center;
            padding: 20px 0;
            border-bottom: 1px solid #dee2e6;
            background: #fff;
        }
        .header img {
            max-width: 150px;
        }
        .header h1 {
            font-size: 28px;
            color: #007bff;
            margin: 10px 0 5px;
            font-weight: 600;
        }
        .header span {
            font-family: Times New Roman, serif;
            font-size: 20px;
            color: #007bff;
            display: block;
        }
        .content {
            padding: 20px;
        }
        .content h2 {
            font-size: 24px;
            color: #2c3e50;
            text-align: center;
            margin-bottom: 20px;
            font-weight: 600;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        th, td {
            padding: 10px;
            border: 1px solid #dee2e6;
            text-align: left;
        }
        th {
            background: #f0f0f0;
            font-weight: 700;
            text-transform: uppercase;
        }
        .footer {
            text-align: center;
            padding: 10px 0;
            border-top: 1px solid #dee2e6;
            font-size: 12px;
            color: #6c757d;
            background: #fff;
        }
        @media print {
            body {
                background: #fff;
            }
            .container {
                box-shadow: none;
                border: 1px solid #dee2e6;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <img src="{{ absolute_url(asset('assets/img/EVENTORA.png')) }}" alt="Eventora Logo">
            <span>Votre Événement</span>
            <h1>Réservation Pack #{{ reservation.IDReservationPack }}</h1>
        </div>
        <div class="content">
            <h2>Détails de la Réservation</h2>
            <table>
                <tr>
                    <th>Champ</th>
                    <th>Valeur</th>
                </tr>
                <tr>
                    <td>Nom du Pack</td>
                    <td>{{ reservation.pack ? reservation.pack.nomPack|slice(0, 100)|e|default('N/A') : 'Aucun pack associé' }}</td>
                </tr>
                <tr>
                    <td>Nom</td>
                    <td>{{ reservation.nom|default('N/A') }}</td>
                </tr>
                <tr>
                    <td>Prénom</td>
                    <td>{{ reservation.prenom|default('N/A') }}</td>
                </tr>
                <tr>
                    <td>Email</td>
                    <td>{{ reservation.email|default('N/A') }}</td>
                </tr>
                <tr>
                    <td>Numéro de téléphone</td>
                    <td>{{ reservation.numtel|default('N/A') }}</td>
                </tr>
                <tr>
                    <td>Description</td>
                    <td>{{ reservation.description|slice(0, 500)|e|default('N/A') }}</td>
                </tr>
                <tr>
                    <td>Date</td>
                    <td>{{ reservation.date ? reservation.date|date('Y-m-d') : 'N/A' }}</td>
                </tr>
            </table>
        </div>
        <div class="footer">
            <p>© 2025 Eventora. Tous droits réservés. | Généré le {{ 'now'|date('Y-m-d H:i:s') }}</p>
        </div>
    </div>
</body>
</html>