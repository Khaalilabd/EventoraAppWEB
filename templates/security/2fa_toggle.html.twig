{% extends 'base.html.twig' %}

{% block title %}Double authentification{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header">
                    <h3 class="text-center">Double authentification</h3>
                </div>
                <div class="card-body">
                    <p class="text-center">
                        La double authentification ajoute une couche de sécurité supplémentaire à votre compte.
                        Lorsqu'elle est activée, vous devrez entrer un code de vérification envoyé par email
                        à chaque connexion.
                    </p>

                    {% for message in app.flashes('success') %}
                        <div class="alert alert-success">
                            {{ message }}
                        </div>
                    {% endfor %}

                    <form method="post">
                        <div class="form-group text-center">
                            <div class="custom-control custom-switch">
                                <input type="checkbox" class="custom-control-input" id="enable2fa" 
                                       name="enable" value="true" {{ isEnabled ? 'checked' : '' }}>
                                <label class="custom-control-label" for="enable2fa">
                                    Activer la double authentification
                                </label>
                            </div>
                        </div>
                        <div class="form-group mt-3">
                            <button type="submit" class="btn btn-primary w-100">Enregistrer</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %} 