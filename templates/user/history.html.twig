<!DOCTYPE html>
<html class="no-js" lang="fr">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>{% block title %}Historique - Eventora{% endblock %}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 128 128'><text y='1.2em' font-size='96'>⚫️</text><text y='1.3em' x='0.2em' font-size='76' fill='%23fff'>sf</text></svg>">

    {% block stylesheets %}
        <link href="https://fonts.googleapis.com/css?family=Work+Sans:400,300,600,400italic,700" rel="stylesheet" type="text/css">
        <link href="https://fonts.googleapis.com/css?family=Sacramento" rel="stylesheet">
        <link rel="stylesheet" href="{{ asset('css/animate.css') }}">
        <link rel="stylesheet" href="{{ asset('css/icomoon.css') }}">
        <link rel="stylesheet" href="{{ asset('css/bootstrap.css') }}">
        <link rel="stylesheet" href="{{ asset('css/magnific-popup.css') }}">
        <link rel="stylesheet" href="{{ asset('css/owl.carousel.min.css') }}">
        <link rel="stylesheet" href="{{ asset('css/owl.theme.default.min.css') }}">
        <link rel="stylesheet" href="{{ asset('css/style1.css') }}">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" />
        <style>
            /* Général */
            body {
                background: linear-gradient(135deg, #F5F5DC 0%, #8B4513 100%); /* Beige to brown gradient */
                background-size: cover;
                background-attachment: fixed;
                min-height: 100vh;
                margin: 0;
                padding: 0;
                position: relative;
                font-family: 'Work Sans', sans-serif;
                color: #5C4033; /* Darker brown */
            }
            #page {
                min-height: 100vh;
                display: flex;
                flex-direction: column;
                padding-top: 70px;
            }

            /* Navigation */
            .fh5co-nav {
                position: fixed;
                top: 0;
                width: 100%;
                z-index: 1000;
                background: rgba(255, 255, 255, 0.9);
                box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
                backdrop-filter: blur(10px);
            }
            .fh5co-nav .container {
                display: flex;
                align-items: center;
                justify-content: space-between;
                padding: 10px 15px;
                max-width: 1200px;
                margin: 0 auto;
            }
            .fh5co-logo a {
                font-weight: 700;
                font-size: 24px;
                color: #934D1A;
                text-decoration: none;
            }
            .user-circle {
                display: inline-block;
                width: 40px;
                height: 40px;
                line-height: 40px;
                text-align: center;
                border-radius: 50%;
                background-color: #f0f0f0;
                color: #333;
                font-size: 20px;
                vertical-align: middle;
                margin-right: 8px;
            }
            .user-circle img {
                width: 100%;
                height: 100%;
                object-fit: cover;
                border-radius: 50%;
            }
            .fh5co-nav .menu-1 ul {
                display: flex;
                align-items: center;
                justify-content: flex-end;
                margin: 0;
                padding: 0;
                list-style: none;
            }
            .fh5co-nav .menu-1 ul li {
                margin: 0 15px;
                position: relative;
            }
            .fh5co-nav .menu-1 ul li a {
                font-size: 16px;
                color: #333;
                text-transform: uppercase;
                text-decoration: none;
                transition: color 0.3s ease;
            }
            .fh5co-nav .menu-1 ul li a:hover {
                color: #934D1A;
            }
            .fh5co-nav .menu-1 ul li.active a {
                color: #934D1A;
                font-weight: 600;
            }
            .fh5co-nav .menu-1 ul li.has-dropdown .dropdown {
                display: none;
                position: absolute;
                top: 100%;
                right: 0;
                background-color: #fff;
                box-shadow: 0 2px 5px rgba(0,0,0,0.1);
                list-style: none;
                padding: 10px 0;
                min-width: 150px;
                z-index: 1001;
                visibility: hidden;
                opacity: 0;
                transition: opacity 0.2s ease, visibility 0.2s ease;
            }
            .fh5co-nav .menu-1 ul li.has-dropdown:hover .dropdown {
                display: block;
                visibility: visible;
                opacity: 1;
            }
            .fh5co-nav .menu-1 ul li.has-dropdown .dropdown li a {
                padding: 10px 20px;
                font-size: 14px;
                text-transform: none;
                color: #333;
            }
            .fh5co-nav .menu-1 ul li.has-dropdown .dropdown li a:hover {
                color: #8B4513;
            }
            .menu-toggle {
                display: none;
                font-size: 24px;
                cursor: pointer;
                color: #333;
                padding: 10px;
            }

            /* Section contenu */
            .content-section {
                padding: 100px 0 50px;
                flex: 1 0 auto;
            }
            .content-card {
                background: rgba(255, 255, 255, 0.95);
                border-radius: 20px;
                padding: 30px;
                max-width: 1000px;
                margin: 0 auto;
                box-shadow: 0 8px 30px rgba(0, 0, 0, 0.1);
                border: 1px solid #D2B48C; /* Light beige border */
                transition: transform 0.3s ease, box-shadow 0.3s ease;
            }
            .content-card:hover {
                transform: translateY(-5px);
                box-shadow: 0 12px 40px rgba(0, 0, 0, 0.15);
            }
            .content-card h2 {
                font-size: 26px;
                color: #8B4513; /* Brown */
                margin-bottom: 15px;
                text-align: center;
                display: flex;
                align-items: center;
                justify-content: center;
                gap: 10px;
                animation: slideIn 0.5s ease-out;
            }
            .content-card h2::after {
                content: '📜';
                font-size: 20px;
                color: #DAA520; /* Golden accent */
            }
            .content-card .subtitle {
                font-size: 16px;
                color: #5C4033; /* Darker brown */
                text-align: center;
                margin-bottom: 25px;
            }
            @keyframes slideIn {
                from {
                    opacity: 0;
                    transform: translateX(-20px);
                }
                to {
                    opacity: 1;
                    transform: translateX(0);
                }
            }

            /* Onglets */
            .nav-tabs {
                border-bottom: 2px solid #D2B48C; /* Light beige */
                margin-bottom: 30px;
                display: flex;
                justify-content: center;
            }
            .nav-tabs li {
                margin: 0 10px;
            }
            .nav-tabs li a {
                color: #5C4033; /* Darker brown */
                font-size: 16px;
                padding: 10px 20px;
                border: none;
                border-bottom: 3px solid transparent;
                transition: all 0.3s ease;
                text-decoration: none;
            }
            .nav-tabs li.active a {
                color: #8B4513; /* Brown */
                border-bottom: 3px solid #8B4513;
                font-weight: 600;
            }
            .nav-tabs li a:hover {
                color: #8B4513;
                border-bottom: 3px solid #8B4513;
            }

            /* Tableau */
            .history-table {
                width: 100%;
                border-collapse: separate;
                border-spacing: 0;
                background: #FAF0E6; /* Very light beige */
                border-radius: 10px;
                overflow: hidden;
                box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
                margin-bottom: 20px;
            }
            .history-table thead {
                background: linear-gradient(135deg, #D2B48C 0%, #8B4513 100%); /* Beige to brown gradient */
            }
            .history-table th {
                color: #fff;
                font-weight: 600;
                padding: 15px;
                text-align: left;
                font-size: 14px;
                text-transform: uppercase;
                letter-spacing: 0.5px;
            }
            .history-table tbody tr {
                transition: background 0.3s ease;
            }
            .history-table tbody tr:nth-child(even) {
                background: #F5F5DC; /* Light beige */
            }
            .history-table tbody tr:hover {
                background: #EDE4D3; /* Slightly darker beige */
            }
            .history-table td {
                padding: 15px;
                border-bottom: 1px solid #D2B48C; /* Light beige */
                color: #5C4033; /* Darker brown */
                font-size: 14px;
                vertical-align: middle;
            }
            .history-table td .label {
                font-weight: 500;
            }
            .history-table td .btn {
                padding: 6px 12px;
                font-size: 13px;
            }
            .history-table td .stars {
                white-space: nowrap;
            }

            /* Étiquettes */
            .label {
                padding: 6px 12px;
                border-radius: 12px;
                font-size: 13px;
                display: inline-block;
                min-width: 80px;
                text-align: center;
            }
            .label-success {
                background: #28a745; /* Vert pour Résolue, Validé, Oui */
                color: #ffffff;
            }
            .label-warning {
                background: #ffb300; /* Jaune pour En attente */
                color: #333;
            }
            .label-danger {
                background: #dc3545; /* Rouge pour Rejetée, Refusé, Non */
                color: #ffffff;
            }

            /* Étoiles */
            .stars {
                color: #A0522D; /* Medium brown */
                font-size: 14px;
            }
            .stars .filled {
                color: #DAA520; /* Golden */
                animation: starGlow 0.5s ease;
            }
            @keyframes starGlow {
                0% { transform: scale(1); }
                50% { transform: scale(1.3); }
                100% { transform: scale(1); }
            }

            /* Boutons */
            .btn {
                padding: 12px;
                border-radius: 50px;
                font-size: 16px;
                font-weight: 500;
                transition: all 0.3s ease;
                position: relative;
                overflow: hidden;
                border: none;
            }
            .btn-primary {
                background: linear-gradient(90deg, #D2B48C, #8B4513); /* Beige to brown gradient */
                color: #fff;
            }
            .btn-primary:hover {
                background: linear-gradient(90deg, #C19A6B, #5C4033); /* Slightly darker gradient */
                transform: scale(1.05);
            }
            .btn-secondary {
                background: #EDE4D3; /* Light beige */
                color: #5C4033; /* Darker brown text */
            }
            .btn-secondary:hover {
                background: #D2B48C; /* Medium beige */
                transform: scale(1.05);
            }
            .btn:focus {
                outline: 2px solid #8B4513;
                outline-offset: 2px;
            }

            /* Détails */
            .reclamation-details, .feedback-details, .reservation-details {
                font-size: 15px;
                background: #FAF0E6; /* Very light beige */
                padding: 20px;
                border-radius: 10px;
                border: 1px solid #D2B48C;
                margin=C2=A0margin-bottom: 20px;
            }
            .reclamation-details h3, .feedback-details h3, .reservation-details h3 {
                color: #8B4513; /* Brown */
                font-size: 22px;
                margin-bottom: 20px;
                display: flex;
                align-items: center;
                gap: 10px;
                animation: slideIn 0.5s ease-out;
            }
            .reclamation-details h3::after, .feedback-details h3::after, .reservation-details h3::after {
                content: '✨';
                font-size: 18px;
                color: #DAA520; /* Golden accent */
            }
            .reclamation-details p, .feedback-details p, .reservation-details p {
                margin: 12px 0;
                padding: 12px;
                background: #F5F5DC; /* Light beige */
                border-radius: 6px;
                border-left: 4px solid #8B4513; /* Brown */
            }
            .reclamation-details strong, .feedback-details strong, .reservation-details strong {
                color: #5C4033; /* Darker brown */
                margin-right: 8px;
                font-weight: 600;
            }
            .feedback-details img {
                max-width: 300px;
                border-radius: 8px;
                margin-top: 10px;
                display: block;
                border: 1px solid #D2B48C;
            }

            /* Pagination */
            .pagination {
                justify-content: center;
                margin-top: 25px;
            }
            .pagination li {
                margin: 0 5px;
            }
            .pagination li a {
                color: #5C4033; /* Darker brown */
                border: 1px solid #D2B48C; /* Light beige */
                padding: 8px 12px;
                border-radius: 6px;
                text-decoration: none;
                transition: all 0.3s ease;
                font-size: 14px;
            }
            .pagination li.active a {
                background: #8B4513; /* Brown */
                color: #ffffff;
                border-color: #8B4513;
            }
            .pagination li a:hover {
                background: #8B4513;
                color: #ffffff;
                border-color: #8B4513;
            }
            .pagination li.disabled a {
                color: #A0522D; /* Medium brown */
                cursor: not-allowed;
                border-color: #D2B48C;
                background: #F5F5DC; /* Light beige */
            }

            /* Filtre */
            .filter-form {
                margin-bottom: 20px;
                text-align: center;
            }
            .filter-form select {
                padding: 12px;
                border: none;
                border-bottom: 2px solid #D2B48C; /* Light beige */
                background: #FAF0E6; /* Very light beige */
                border-radius: 0;
                font-size: 14px;
                color: #5C4033; /* Darker brown */
                transition: border-bottom 0.3s ease;
            }
            .filter-form select:focus {
                border-bottom: 2px solid #8B4513; /* Brown */
                outline: none;
            }

            /* Message vide */
            .text-center p {
                color: #5C4033;
                font-size: 16px;
                margin-bottom: 20px;
            }

            /* Pied de page */
            #fh5co-footer {
                background: rgba(255, 255, 255, 0.9);
                padding: 20px 0;
                margin-top: auto;
            }
            #fh5co-footer p {
                color: #5C4033;
                font-size: 14px;
                text-align: center;
            }
            #fh5co-footer a {
                color: #8B4513;
                text-decoration: none;
            }
            #fh5co-footer a:hover {
                text-decoration: underline;
            }

            /* Responsive */
            @media (max-width: 768px) {
                .menu-toggle {
                    display: block;
                }
                .fh5co-nav .menu-1 ul {
                    display: none;
                    flex-direction: column;
                    position: absolute;
                    top: 60px;
                    left: 0;
                    width: 100%;
                    background-color: #fff;
                    box-shadow: 0 2px 5px rgba(0,0,0,0.1);
                    padding: 20px 0;
                }
                .fh5co-nav .menu-1 ul.active {
                    display: flex;
                }
                .fh5co-nav .menu-1 ul li {
                    margin: 10px 0;
                    text-align: center;
                }
                .fh5co-nav .menu-1 ul li.has-dropdown .dropdown {
                    position: static;
                    display: none;
                    box-shadow: none;
                    background-color: #F5F5DC; /* Light beige */
                }
                .fh5co-nav .menu-1 ul li.has-dropdown:hover .dropdown {
                    display: none !important;
                }
                .fh5co-nav .menu-1 ul li.has-dropdown.active .dropdown {
                    display: block !important;
                }
                .content-card {
                    padding: 20px;
                    max-width: 95%;
                }
                .content-card h2 {
                    font-size: 22px;
                }
                .history-table {
                    font-size: 13px;
                }
                .history-table th, .history-table td {
                    padding: 10px;
                    font-size: 13px;
                }
                .history-table td:nth-child(3), .history-table td:nth-child(4), .history-table td:nth-child(5) {
                    max-width: 100px;
                    white-space: nowrap;
                    overflow: hidden;
                    text-overflow: ellipsis;
                }
                .nav-tabs li a {
                    font-size: 14px;
                    padding: 8px 15px;
                }
                .reclamation-details p, .feedback-details p, .reservation-details p {
                    font-size: 14px;
                    padding: 10px;
                }
                .feedback-details img {
                    max-width: 100%;
                }
                .filter-form select {
                    width: 100%;
                    font-size: 13px;
                }
            }
        </style>
    {% endblock %}
</head>
<body>
    <div id="page">
        <nav class="fh5co-nav" role="navigation">
            <div class="container">
                <div class="fh5co-logo">
                    <a href="{{ path('app_home') }}">Eventora</a>
                </div>
                <div class="menu-toggle">
                    <i class="fas fa-bars"></i>
                </div>
                <div class="menu-1">
                    <ul>
                        <li class="{{ app.request.attributes.get('_route') == 'app_home' ? 'active' : '' }}"><a href="{{ path('app_home') }}">Home</a></li>
                        <li class="has-dropdown">
                            <a href="#">Réservation</a>
                            <ul class="dropdown">
                                <li class="{{ app.request.attributes.get('_route') == 'user_reservation_personnalise_new' ? 'active' : '' }}"><a href="{{ path('user_reservation_personnalise_new') }}">Réservation personnalisée</a></li>
                                <li class="{{ app.request.attributes.get('_route') == 'user_reservation_pack_new' ? 'active' : '' }}"><a href="{{ path('user_reservation_pack_new') }}">Réservation Pack</a></li>
                            </ul>
                        </li>
                        <li class="{{ app.request.attributes.get('_route') == 'user_packs' ? 'active' : '' }}"><a href="{{ path('user_packs') }}">Packs</a></li>
                        <li class="has-dropdown">
                            <a href="#">Services</a>
                            <ul class="dropdown">
                                <li class="{{ app.request.attributes.get('_route') == 'user_services' ? 'active' : '' }}"><a href="{{ path('user_services') }}">Service</a></li>
                                <li class="{{ app.request.attributes.get('_route') == 'user_sponsor' ? 'active' : '' }}"><a href="{{ path('user_sponsor') }}">Partenaire</a></li>
                            </ul>
                        </li>
                        <li class="{{ app.request.attributes.get('_route') == 'app_reclamation_new' ? 'active' : '' }}"><a href="{{ path('app_reclamation_new') }}">Réclamation</a></li>
                        <li class="{{ app.request.attributes.get('_route') == 'app_feedback_new' ? 'active' : '' }}"><a href="{{ path('app_feedback_new') }}">Feedback</a></li>
                        <li class="{{ app.request.attributes.get('_route') == 'user_chat_ai' ? 'active' : '' }}"><a href="{{ path('user_chat_ai') }}">Parler avec Evi</a></li>
                        <li class="has-dropdown">
                            <a href="#">
                                <span class="user-circle">
                                    {% if app.user and app.user.image %}
                                        <img src="{{ asset('Uploads/images/' ~ app.user.image) }}" alt="Profil" aria-hidden="true">
                                    {% else %}
                                        <i class="icon-user"></i>
                                    {% endif %}
                                </span>
                                {{ app.user ? app.user.prenom|default('Compte') : 'Compte' }}
                            </a>
                            <ul class="dropdown">
                                {% if app.user %}
                                    <li><a href="{{ path('app_settings') }}">Paramètres</a></li>
                                    <li><a href="{{ path('app_home') }}">Notifications</a></li>
                                    <li class="{{ app.request.attributes.get('_route') starts with 'app_user_history' ? 'active' : '' }}"><a href="{{ path('app_user_history') }}">Historique</a></li>
                                    {% if is_granted('ROLE_ADMIN') %}
                                        <li><a href="{{ path('admin_dashboard') }}">Dashboard</a></li>
                                    {% endif %}
                                    <li><a href="{{ path('app_logout') }}">Déconnexion</a></li>
                                {% else %}
                                    <li><a href="{{ path('app_auth') }}">Connexion</a></li>
                                    <li><a href="{{ path('app_auth') }}">Inscription</a></li>
                                {% endif %}
                            </ul>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>

        <section class="content-section" role="main">
            <div class="container">
                <div class="content-card animate-box">
                    <h2>Votre Historique</h2>
                    <div class="subtitle">Consultez vos réclamations, feedbacks et réservations</div>

                    <!-- Onglets pour les sections -->
                    <ul class="nav nav-tabs" role="tablist">
                        <li class="{{ section == 'reclamations' ? 'active' : '' }}" role="presentation"><a href="{{ path('app_user_history') }}" role="tab" aria-selected="{{ section == 'reclamations' ? 'true' : 'false' }}">Réclamations</a></li>
                        <li class="{{ section == 'feedbacks' ? 'active' : '' }}" role="presentation"><a href="{{ path('app_user_history_feedbacks') }}" role="tab" aria-selected="{{ section == 'feedbacks' ? 'true' : 'false' }}">Feedbacks</a></li>
                        <li class="{{ section == 'reservations' ? 'active' : '' }}" role="presentation"><a href="{{ path('app_user_history_reservations') }}" role="tab" aria-selected="{{ section == 'reservations' ? 'true' : 'false' }}">Réservations</a></li>
                    </ul>

                    {% if section == 'reclamations' %}
                        {% if selected_reclamation %}
                            <!-- Détails de la réclamation -->
                            <div class="reclamation-details" role="region" aria-labelledby="reclamation-title">
                                <h3 id="reclamation-title">Détails de la Réclamation #{{ selected_reclamation.id }}</h3>
                                <p><strong>Titre :</strong> {{ selected_reclamation.titre }}</p>
                                <p><strong>Type :</strong> {{ selected_reclamation.Type }}</p>
                                <p><strong>Description :</strong> {{ selected_reclamation.description }}</p>
                                <p><strong>Statut :</strong>
                                    <span class="label {{ selected_reclamation.statut == 'Resolue' ? 'label-success' : (selected_reclamation.statut == 'Rejetée' ? 'label-danger' : 'label-warning') }}">
                                        {{ selected_reclamation.statut }}
                                    </span>
                                </p>
                                <p><strong>Date :</strong> {{ selected_reclamation.date|date('d/m/Y H:i') }}</p>
                                <div class="text-center">
                                    <a href="{{ path('app_user_history') }}" class="btn btn-secondary">Retour à l'historique</a>
                                </div>
                            </div>
                        {% else %}
                            <!-- Liste des réclamations -->
                            {% if reclamations|length == 0 %}
                                <p class="text-center">Aucune réclamation trouvée.</p>
                                <div class="text-center">
                                    <a href="{{ path('app_reclamation_new') }}" class="btn btn-primary">Soumettre une réclamation</a>
                                </div>
                            {% else %}
                                <div class="table-responsive">
                                    <table class="history-table" role="grid">
                                        <thead>
                                            <tr>
                                                <th scope="col">Date</th>
                                                <th scope="col">Titre</th>
                                                <th scope="col">Type</th>
                                                <th scope="col">Description</th>
                                                <th scope="col">Statut</th>
                                                <th scope="col">Actions</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            {% for reclamation in reclamations %}
                                                <tr class="animate-box" data-animate-effect="fadeIn">
                                                    <td>{{ reclamation.date|date('d/m/Y H:i') }}</td>
                                                    <td>{{ reclamation.titre }}</td>
                                                    <td>{{ reclamation.Type }}</td>
                                                    <td title="{{ reclamation.description }}">{{ reclamation.description|u.truncate(60, '...') }}</td>
                                                    <td>
                                                        <span class="label {{ reclamation.statut == 'Resolue' ? 'label-success' : (reclamation.statut == 'Rejetée' ? 'label-danger' : 'label-warning') }}">
                                                            {{ reclamation.statut }}
                                                        </span>
                                                    </td>
                                                    <td>
                                                        <a href="{{ path('app_reclamation_show', {'id': reclamation.id}) }}" class="btn btn-primary btn-sm" aria-label="Voir la réclamation {{ reclamation.titre }}">Voir</a>
                                                    </td>
                                                </tr>
                                            {% endfor %}
                                        </tbody>
                                    </table>
                                </div>
                                <!-- Pagination -->
                                <div class="text-center">
                                    {{ knp_pagination_render(reclamations) }}
                                </div>
                            {% endif %}
                        {% endif %}
                    {% elseif section == 'feedbacks' %}
                        {% if selected_feedback %}
                            <!-- Détails du feedback -->
                            <div class="feedback-details" role="region" aria-labelledby="feedback-title">
                                <h3 id="feedback-title">Détails du Feedback #{{ selected_feedback.ID }}</h3>
                                <p><strong>Note :</strong> <span class="stars">
                                    {% for i in 1..selected_feedback.Vote %}
                                        <span class="filled">★</span>
                                    {% endfor %}
                                    {% for i in 1..(5 - selected_feedback.Vote) %}
                                        <span>☆</span>
                                    {% endfor %}
                                </span></p>
                                <p><strong>Description :</strong> {{ selected_feedback.Description }}</p>
                                <p><strong>Recommandation :</strong>
                                    <span class="label {{ selected_feedback.Recommend == 'Oui' ? 'label-success' : 'label-danger' }}">
                                        {{ selected_feedback.Recommend }}
                                    </span>
                                </p>
                                <p><strong>Date :</strong> {{ selected_feedback.date|date('d/m/Y H:i') }}</p>
                                {% if selected_feedback.Souvenirs %}
                                    <p><strong>Souvenir :</strong><br>
                                        <img src="{{ path('app_feedback_image', {'id': selected_feedback.ID}) }}" alt="Souvenir du feedback">
                                    </p>
                                {% endif %}
                                <div class="text-center">
                                    <a href="{{ path('app_user_history_feedbacks') }}" class="btn btn-secondary">Retour à l'historique</a>
                                </div>
                            </div>
                        {% else %}
                            <!-- Liste des feedbacks -->
                            {% if feedbacks|length == 0 %}
                                <p class="text-center">Aucun feedback trouvé.</p>
                                <div class="text-center">
                                    <a href="{{ path('app_feedback_new') }}" class="btn btn-primary">Soumettre un feedback</a>
                                </div>
                            {% else %}
                                <div class="table-responsive">
                                    <table class="history-table" role="grid">
                                        <thead>
                                            <tr>
                                                <th scope="col">Date</th>
                                                <th scope="col">Note</th>
                                                <th scope="col">Description</th>
                                                <th scope="col">Recommandation</th>
                                                <th scope="col">Actions</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            {% for feedback in feedbacks %}
                                                <tr class="animate-box" data-animate-effect="fadeIn">
                                                    <td>{{ feedback.date|date('d/m/Y H:i') }}</td>
                                                    <td><span class="stars">
                                                        {% for i in 1..feedback.Vote %}
                                                            <span class="filled">★</span>
                                                        {% endfor %}
                                                        {% for i in 1..(5 - feedback.Vote) %}
                                                            <span>☆</span>
                                                        {% endfor %}
                                                    </span></td>
                                                    <td title="{{ feedback.Description }}">{{ feedback.Description|u.truncate(60, '...') }}</td>
                                                    <td>
                                                        <span class="label {{ feedback.Recommend == 'Oui' ? 'label-success' : 'label-danger' }}">
                                                            {{ feedback.Recommend }}
                                                        </span>
                                                    </td>
                                                    <td>
                                                        <a href="{{ path('app_feedback_show', {'id': feedback.ID}) }}" class="btn btn-primary btn-sm" aria-label="Voir le feedback #{{ feedback.ID }}">Voir</a>
                                                    </td>
                                                </tr>
                                            {% endfor %}
                                        </tbody>
                                    </table>
                                </div>
                                <!-- Pagination -->
                                <div class="text-center">
                                    {{ knp_pagination_render(feedbacks) }}
                                </div>
                            {% endif %}
                        {% endif %}
                    {% elseif section == 'reservations' %}
                        {% if selected_reservation %}
                            <!-- Détails de la réservation -->
                            <div class="reservation-details" role="region" aria-labelledby="reservation-title">
                                <h3 id="reservation-title">Détails de la Réservation {{ reservation_type == 'pack' ? 'Pack' : 'Personnalisée' }} #{{ selected_reservation.IDReservationPack ?: selected_reservation.IDReservationPersonalise }}</h3>
                                <p><strong>Nom :</strong> {{ selected_reservation.nom }}</p>
                                <p><strong>Prénom :</strong> {{ selected_reservation.prenom }}</p>
                                <p><strong>Date :</strong> {{ selected_reservation.date|date('d/m/Y H:i') }}</p>
                                <p><strong>Description :</strong> {{ selected_reservation.description }}</p>
                                <p><strong>Statut :</strong>
                                    <span class="label {{ selected_reservation.status == 'Validé' ? 'label-success' : (selected_reservation.status == 'Refusé' ? 'label-danger' : 'label-warning') }}">
                                        {{ selected_reservation.status }}
                                    </span>
                                </p>
                                {% if reservation_type == 'pack' and selected_reservation.pack %}
                                    <p><strong>Pack :</strong> {{ selected_reservation.pack.nomPack }}</p>
                                {% elseif reservation_type == 'personnalise' and selected_reservation.services|length > 0 %}
                                    <p><strong>Services :</strong>
                                        <ul>
                                            {% for service in selected_reservation.services %}
                                                <li>{{ service.nom }}</li>
                                            {% endfor %}
                                        </ul>
                                    </p>
                                {% endif %}
                                <div class="text-center">
                                    <a href="{{ path('app_user_history_reservations', {'type': reservation_type}) }}" class="btn btn-secondary">Retour à l'historique</a>
                                    <a href="{{ path(reservation_type == 'pack' ? 'user_reservation_pack_pdf' : 'user_reservation_personnalise_pdf', {'id': selected_reservation.IDReservationPack ?: selected_reservation.IDReservationPersonalise}) }}" class="btn btn-primary">Télécharger PDF</a>
                                </div>
                            </div>
                        {% else %}
                            <!-- Filtre pour les réservations -->
                            <div class="filter-form">
                                <form action="{{ path('app_user_history_reservations') }}" method="get" role="form">
                                    <select name="type" onchange="this.form.submit()" aria-label="Filtrer les réservations par type">
                                        <option value="" {% if reservation_type is null %}selected{% endif %}>Tous</option>
                                        <option value="pack" {% if reservation_type == 'pack' %}selected{% endif %}>Pack</option>
                                        <option value="personnalise" {% if reservation_type == 'personnalise' %}selected{% endif %}>Personnalisée</option>
                                    </select>
                                </form>
                            </div>
                            <!-- Liste des réservations -->
                            {% if reservations|length == 0 %}
                                <p class="text-center">Aucune réservation trouvée.</p>
                                <div class="text-center">
                                    <a href="{{ path('user_reservation_pack_new') }}" class="btn btn-primary">Réserver un pack</a>
                                    <a href="{{ path('user_reservation_personnalise_new') }}" class="btn btn-primary">Réserver personnalisée</a>
                                </div>
                            {% else %}
                                <div class="table-responsive">
                                    <table class="history-table" role="grid">
                                        <thead>
                                            <tr>
                                                <th scope="col">Type</th>
                                                <th scope="col">Date</th>
                                                <th scope="col">Nom</th>
                                                <th scope="col">Prénom</th>
                                                <th scope="col">Description</th>
                                                <th scope="col">Statut</th>
                                                <th scope="col">Actions</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            {% for reservation in reservations %}
                                                <tr class="animate-box" data-animate-effect="fadeIn">
                                                    <td>{{ reservation.type }}</td>
                                                    <td>{{ reservation.date|date('d/m/Y H:i') }}</td>
                                                    <td>{{ reservation.nom }}</td>
                                                    <td>{{ reservation.prenom }}</td>
                                                    <td title="{{ reservation.description }}">{{ reservation.description|u.truncate(60, '...') }}</td>
                                                    <td>
                                                        <span class="label {{ reservation.status == 'Validé' ? 'label-success' : (reservation.status == 'Refusé' ? 'label-danger' : 'label-warning') }}">
                                                            {{ reservation.status }}
                                                        </span>
                                                    </td>
                                                    <td>
                                                        <a href="{{ path('app_reservation_show', {'id': reservation.IDReservationPack ?: reservation.IDReservationPersonalise, 'type': reservation.type == 'Pack' ? 'pack' : 'personnalise'}) }}" class="btn btn-primary btn-sm" aria-label="Voir la réservation {{ reservation.type }} de {{ reservation.nom }}">Voir</a>
                                                    </td>
                                                </tr>
                                            {% endfor %}
                                        </tbody>
                                    </table>
                                </div>
                                <!-- Pagination -->
                                <div class="text-center">
                                    {{ knp_pagination_render(reservations) }}
                                </div>
                            {% endif %}
                        {% endif %}
                    {% endif %}
                </div>
            </div>
        </section>

        <footer id="fh5co-footer" role="contentinfo">
            <div class="container">
                <div class="row copyright">
                    <div class="col-md-12 text-center">
                        <p>
                            <small class="block">© {{ 'now'|date('Y') }} Eventora. Tous droits réservés.</small>
                            <small class="block">Conçu par <a href="http://freehtml5.co/" target="_blank" rel="nofollow">FREEHTML5.co</a></small>
                        </p>
                    </div>
                </div>
            </div>
        </footer>
    </div>

    {% block javascripts %}
        <script src="https://code.jquery.com/jquery-3.6.0.min.js" integrity="sha256-/xUj+3OJU5yExlq6GSYGSHk7tPXikynS7ogEvDej/m4=" crossorigin="anonymous"></script>
        <script src="{{ asset('js/bootstrap.min.js') }}"></script>
        <script>
            if (typeof jQuery === 'undefined') {
                console.error("jQuery n'est pas chargé. Problème avec le CDN ou le réseau.");
            } else {
                console.log("jQuery est chargé avec succès");
                jQuery(document).ready(function($) {
                    $('.menu-toggle').click(function() {
                        console.log("Clic sur menu-toggle détecté");
                        $('.fh5co-nav .menu-1 ul').toggleClass('active');
                    });

                    $('.fh5co-nav .menu-1 ul li.has-dropdown > a').click(function(e) {
                        if ($(window).width() <= 768) {
                            console.log("Clic sur dropdown détecté");
                            e.preventDefault();
                            $(this).parent().toggleClass('active');
                        }
                    });

                    $('.fh5co-nav .menu-1 ul li.has-dropdown').hover(
                        function() {
                            console.log("Survol détecté sur un élément has-dropdown");
                            $(this).find('.dropdown').css({
                                'display': 'block',
                                'visibility': 'visible',
                                'opacity': '1'
                            });
                        },
                        function() {
                            console.log("Fin du survol");
                            $(this).find('.dropdown').css({
                                'display': 'none',
                                'visibility': 'hidden',
                                'opacity': '0'
                            });
                        }
                    );
                });
            }
        </script>
    {% endblock %}
</body>
</html>